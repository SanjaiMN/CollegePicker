<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.collegepicker">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".GraphActivity" />
        <activity android:name=".AhpProcessing" />
        <activity android:name=".AhpResult" />
        <activity
            android:name=".SplashScreen"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AhpCollegeInput"
            android:parentActivityName=".collegelist" />
        <activity
            android:name=".InsideRecyclerView"
            />
        <activity
            android:name=".profile"
            android:parentActivityName=".collegelist" />
        <activity
            android:name=".passwordreset"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".collegelist" />
        <activity
            android:name=".afterlogin"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>